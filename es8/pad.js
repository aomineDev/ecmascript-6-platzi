// 2. Pad

const text = 'hello'

console.log(text.padStart(9, 'hi, ')) // (número de caracteres, caracteres añadidos al inicio)
console.log(text.padEnd(9, ', hi')) // (número de caracteres, caracteres añadidos al final)
